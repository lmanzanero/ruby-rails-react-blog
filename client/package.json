{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://lmanzanero.github.io/ruby-rails-react-blog",
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && gh-pages -d build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:3001/", 
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}